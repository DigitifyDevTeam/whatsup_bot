WHATSAPP_SESSION_PATH=./auth_state
API_URL=http://localhost:8000
# POST /process-message (voice = Whisper + multiple LLM calls); use >= OLLAMA_TIMEOUT_SECONDS × calls.
BACKEND_TIMEOUT_MS=3600000
SKIP_BACKEND=false
PROCESS_OWN_MESSAGES=false
ONLY_GROUP_NAME=
ONLY_SENDER_JID=
